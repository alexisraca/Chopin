<% if @variants.empty? %>
  <tr>
    <td  colspan="5" >
      <h1 class="text-center">
        No hay productos
      </h1>
    </td>
  </tr>
<% else %>
  <%= render partial: "variant", collection: @variants %>
<% end %>