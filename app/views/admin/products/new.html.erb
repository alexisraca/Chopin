<%= simple_form_for [:admin, @product, @product.variants.build] do |f| %>

  <%= render partial: "/shared/form_errors", locals: { instance: @product } %>
    <%= f.input :product_id, class:'form-control', autofocus: true, placeholder: 'id producto'%>
    <%= f.input :name, autofocus: true, placeholder: 'Nombre'%>
    <%= f.input :description, autofocus: true, placeholder: 'Descripción'%>
    <%= f.input :cost, autofocus: true, placeholder: 'Costo'%>
    <%= f.input :price, autofocus: true, placeholder: 'Precio'%>
    <%= f.input :sku, autofocus: true, placeholder: 'Sku'%>

  <%= simple_fields_for :inventories do |i| %>
    <%= i.input :quantity, autofocus: true, placeholder: 'Cantidad' %>
  <% end %>

  <%= f.submit %>
<% end %> 