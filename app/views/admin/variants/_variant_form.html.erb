<div class="panel panel-default variant-row nested-fields" <%= variant_id_data_attr(i.object) %>>
  <div class="panel-body">
    <div class="row">
      <div class="col-md-4">
        <%= i.input :name %>
        <%= i.input :price, input_html: { size: "8"} %>
      </div>
      <div class="col-md-4">
        <%= i.input :sku, input_html: { size: "15"} %>
        <%= i.input :cost, input_html: { size: "8"} %>
      </div>
      <div class="col-md-4">
        <% i.object.inventories.build if i.object.inventories.empty? %>
        <%= i.simple_fields_for :inventories do |s| %>
          <%= s.input :quantity %>
        <% end %>
        <% if i.object.persisted? %>
          <%= link_to admin_product_variant_path(@product, i.object), method: :delete, class: "btn btn-danger pull-right", data: { confirm: "¿Esta seguro que desea borrar la variante de este producto?" }, remote: true do %>
            <%= fa_icon "trash-o" %>
          <% end %>
        <% else %>
          <%= link_to_remove_association (fa_icon "trash-o"), i, class: 'btn btn-danger pull-right' %>
        <% end %>
      </div>
    </div>
  </div>

</div>