<%= simple_form_for [:admin, @statement], url: admin_statement_line_items_path(@statement) do |f| %>
  <%= f.simple_fields_for :line_items do |l| %>
    <%= l.input :variant_id, collection: Variant.all, input_html: { class: "variant-select" } %>
  <% end %>
  <%= f.button :submit %>
<% end %>